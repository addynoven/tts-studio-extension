/**
 * Options / Settings page.
 * Accessible from chrome://extensions → TTS Studio → Details → Extension options.
 *
 * TODO: Phase 2 — Full settings UI with sanitization toggles,
 * highlight customization, keyboard shortcuts, etc.
 */

console.log('[TTS Studio] Options page loaded');
